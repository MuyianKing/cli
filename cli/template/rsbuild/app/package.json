{
  "name": "jk-powering-center",
  "type": "module",
  "version": "0.0.1",
  "private": false,
  "scripts": {
    "dev": "npx rsbuild dev --mode development",
    "build": "npx rsbuild build --mode production",
    "build:analyze": "cross-env BUNDLE_ANALYZE=true rsbuild build --mode production",
    "preview": "rsbuild preview"
  },
  "dependencies": {},
  "devDependencies": {}
}
